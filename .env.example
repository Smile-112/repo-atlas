# GitHub integration runs only on the Repo Atlas server.
# Use a fine-grained token with Contents: Read-only and Metadata: Read-only.
# Do not expose the token with VITE_ prefix or commit this file after filling it.
GITHUB_TOKEN=
PORT=8080
